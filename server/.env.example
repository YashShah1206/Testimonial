# Server API Configuration
PORT=5000
NODE_ENV=development

# Database Connection URL (SQLite for Dev, PostgreSQL / Cloud DB for Production)
DATABASE_URL="file:./dev.db"

# Google Gemini API Key for AI Sentiment Analysis (Optional - falls back to local heuristic if omitted)
GEMINI_API_KEY="your_gemini_api_key_here"
